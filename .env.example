# API Configuration
PORT=3000
NODE_ENV=development

# GitHub Configuration
GITHUB_REPO=your-username/this-repo-name
GITHUB_TOKEN=your-github-personal-access-token

# Target Repository Token (for creating PRs in external repos)
# This should be a GitHub token with 'repo' scope for the target repositories
# Add this as a secret named TARGET_REPO_TOKEN in your GitHub repository settings